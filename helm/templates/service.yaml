apiVersion: v1
kind: Service
metadata:
  name: {{ include "gops-desk.fullname" . }}
  labels:
    {{- include "gops-desk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gops-desk.selectorLabels" . | nindent 4 }}
